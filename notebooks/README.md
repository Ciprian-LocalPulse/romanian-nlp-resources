# Notebook-uri

Pastreaza aici notebook-uri exploratorii mici. Pentru experimente reproductibile, muta logica in `src/` sau `scripts/` si pastreaza notebook-ul doar ca raport.

Reguli:

- Nu comite output-uri mari.
- Nu include date personale.
- Noteaza versiunea datasetului si seedurile.

